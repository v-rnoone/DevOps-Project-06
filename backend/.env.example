# Copy this file to .env and fill in real values for local runs.
# In Docker/K8s, these will be injected via docker-compose env vars / ConfigMaps / Secrets instead.

PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USER=quoteuser
DB_PASSWORD=quotepass
DB_NAME=quotedb
